import styled from 'styled-components/native';

export const TyfDatePickerInputStyled = styled.View`
  width: auto;
  position: relative;
  border: 2px solid;
  border-radius: 4px;
  padding: ${16}px ${8}px;
`;
