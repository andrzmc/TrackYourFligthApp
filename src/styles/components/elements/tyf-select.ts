import styled from 'styled-components/native';

export const TyfSelecStyled = styled.View`
  width: auto;
  position: relative;
  border: 2px solid;
  border-radius: ${12}px;
  padding: ${16}px ${8}px;
`;
