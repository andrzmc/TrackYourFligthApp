import styled from 'styled-components/native';

export const TyfSelecStyled = styled.View`
  width: auto;
  position: relative;
  border: 1px solid;
  border-radius: 4px;
  padding: ${16}px ${8}px;
  flex-direction: row;
`;
