import styled from 'styled-components/native';

export const HomeRequestStyled = styled.View`
  width: 100%;
  padding: ${60}px ${16}px ${16}px ${16}px;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  flex-flow: row wrap;
  position: relative;
  gap: ${8}px;
`;

export const HomeRequestGridStyled = styled.View`
  flex-grow: 1;
  margin-bottom: ${8}px;
`;
